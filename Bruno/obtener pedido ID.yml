info:
  name: obtener pedido ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/pedido/6"
  body:
    type: json
    data: |-
      {
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
